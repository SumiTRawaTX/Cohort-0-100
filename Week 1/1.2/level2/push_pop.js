
const initialArray = [1,2,4,5];
initialArray.push(8);
console.log(initialArray);

const Arr = [3, 9, 0];
Arr.pop();
console.log(Arr);

// shift is use to remove the first element of the array

// unshift is use to add the element in the first position of the array